# Overlays (e.g. local) replace with secretGenerator or external secret management.
# Placeholder only — do not use real values from VCS in production.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: devops-challenge
  labels:
    app.kubernetes.io/component: database
stringData:
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "currencies"
