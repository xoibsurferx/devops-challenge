# Overlays should replace; base holds non-sensitive keys only.
apiVersion: v1
kind: Secret
metadata:
  name: app-env
  namespace: devops-challenge
  labels:
    app.kubernetes.io/component: app
stringData:
  POSTGRES_PRISMA_URL: "postgres://postgres:changeme@postgres:5432/currencies?schema=public"
