apiVersion: v1
kind: Service
metadata:
  name: nextjs
  namespace: devops-challenge
  labels:
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nextjs
  ports:
    - name: http
      port: 80
      targetPort: http
