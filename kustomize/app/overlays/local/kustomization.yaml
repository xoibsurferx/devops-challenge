# Minikube / local: NodePort. App image: docker.io/xoibsurferx/devops-challenge:latest (Docker Hub; from base, see patch-image-pull.yaml).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - path: service-nodeport.yaml
  - path: patch-image-pull.yaml
